<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xk.mapper.MessageMapper">
    <resultMap id="BaseResultMap" type="com.xk.entity.Message">
        <id property="id" column="id"/>
        <result property="senderId" column="sender_id"/>
        <result property="receiverId" column="receiver_id"/>
        <result property="type" column="type"/>
        <result property="content" column="content"/>
        <result property="isRead" column="is_read"/>
        <result property="relatedId" column="related_id"/>
        <result property="createdAt" column="created_at"/>
        <result property="senderName" column="sender_name"/>
        <result property="receiverName" column="receiver_name"/>
    </resultMap>

    <select id="getMessageList" resultMap="BaseResultMap">
        SELECT m.*,
               s.name as sender_name,
               r.name as receiver_name
        FROM message m
        LEFT JOIN user s ON m.sender_id = s.id
        LEFT JOIN user r ON m.receiver_id = r.id
        WHERE m.receiver_id = #{receiverId}
        ORDER BY m.created_at DESC
    </select>

    <select id="getUnreadMessageList" resultMap="BaseResultMap">
        SELECT m.*,
               s.name as sender_name,
               r.name as receiver_name
        FROM message m
        LEFT JOIN user s ON m.sender_id = s.id
        LEFT JOIN user r ON m.receiver_id = r.id
        WHERE m.receiver_id = #{receiverId} AND m.is_read = 0
        ORDER BY m.created_at DESC
    </select>

    <select id="getUnreadCount" resultType="int">
        SELECT COUNT(*) FROM message WHERE receiver_id = #{receiverId} AND is_read = 0
    </select>

    <insert id="insertMessage" parameterType="com.xk.entity.Message" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO message (sender_id, receiver_id, type, content, is_read, related_id, created_at)
        VALUES (#{senderId}, #{receiverId}, #{type}, #{content}, 0, #{relatedId}, NOW())
    </insert>

    <update id="markAsRead">
        UPDATE message SET is_read = 1 WHERE id = #{id}
    </update>

    <update id="markAllAsRead">
        UPDATE message SET is_read = 1 WHERE receiver_id = #{receiverId}
    </update>

    <delete id="deleteMessage">
        DELETE FROM message WHERE id = #{id}
    </delete>

    <select id="getMessageById" resultMap="BaseResultMap">
        SELECT m.*,
               s.name as sender_name,
               r.name as receiver_name
        FROM message m
        LEFT JOIN user s ON m.sender_id = s.id
        LEFT JOIN user r ON m.receiver_id = r.id
        WHERE m.id = #{id}
    </select>

    <select id="getRecentContacts" resultMap="BaseResultMap">
        SELECT m.*,
               s.name as sender_name,
               r.name as receiver_name
        FROM message m
        LEFT JOIN user s ON m.sender_id = s.id
        LEFT JOIN user r ON m.receiver_id = r.id
        WHERE m.sender_id = #{senderId}
          AND m.receiver_id = #{receiverId}
          AND m.type = 'contact_student'
          AND m.created_at >= DATE_SUB(NOW(), INTERVAL #{minutes} MINUTE)
        ORDER BY m.created_at DESC
    </select>

    <select id="findResignRequestMessage" resultMap="BaseResultMap">
        SELECT * FROM message
        WHERE sender_id = #{senderId}
          AND receiver_id = #{receiverId}
          AND type = #{type}
          AND related_id = #{relatedId}
        ORDER BY created_at DESC LIMIT 1
    </select>

    <update id="updateMessageContent">
        UPDATE message SET content = #{content} WHERE id = #{id}
    </update>
</mapper>